START
fn factorial{n} { if {n == 0} { -> 1 } else { -> n * factorial{n - 1} } }
{
    °range = #range{10}
    fn f{n} { -> {factorial{n} - n!} == 0 }
    °success = #all{#for_each{range, f}}
    #drucke{success}
}
END